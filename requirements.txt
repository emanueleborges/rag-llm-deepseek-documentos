# Runtime completo (local) — inclui Docker mínimo + extras opcionais
-r requirements-docker.txt

# Embeddings locais sem Ollama (ONNX ~150 MB extra no build)
fastembed>=0.3.0

# API FastAPI (profile api no Docker instala no startup)
fastapi>=0.115.3,<0.116
uvicorn>=0.25.0,<0.35
starlette>=0.41.2,<0.42

# Extras dev/eval (instale só se precisar):
# pip install langgraph chainlit langfuse ragas sentence-transformers pymupdf pytest black
